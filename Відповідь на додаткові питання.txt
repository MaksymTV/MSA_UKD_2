🔹 1. Які виклики виникли при реалізації динамічного завантаження модулів?
Виклики:

Забезпечення сумісності інтерфейсів між ядром і модулями (усі модулі мають реалізовувати один спільний інтерфейс IModule).

Обробка винятків при завантаженні збірок — модуль може містити помилки, бути не тим форматом, не відповідати інтерфейсу.

Динамічне вивантаження модулів у .NET складне через відсутність прямого механізму для розвантаження збірок із памʼяті без використання AssemblyLoadContext.

Безпека — завантаження сторонніх .dll може бути небезпечним, якщо немає перевірки джерел.

🔹 2. Які аспекти мікроядерної архітектури показали найбільшу ефективність у Task-менеджері?
Найбільш ефективні аспекти:

Модульність: кожен функціонал реалізовано як окремий модуль (завдання, автентифікація, сповіщення), який легко змінити або замінити.

Гнучкість: можна додавати нові функції (модулі) без зміни ядра чи перезапуску програми.

Розширюваність: ядро не залежить від реалізацій модулів, що дозволяє легко масштабувати систему.

Простота тестування: кожен модуль можна протестувати незалежно.

🔹 3. Як вплинула вибрана архітектура на продуктивність і масштабованість системи?
Продуктивність:

Невелике зниження продуктивності під час початкового завантаження модулів через використання рефлексії (Assembly.LoadFrom).

Під час роботи — продуктивність висока, бо модулі працюють незалежно.

Масштабованість:

Висока: можна додати нові модулі, не змінюючи існуючі.

Завдяки слабкому зв’язуванню між ядром і модулями, можна реалізувати підтримку плагінів сторонніх розробників.

🔹 4. Чи можна покращити систему, використовуючи іншу архітектуру? Якщо так, то яку саме?
Так, можна:

Service-Oriented Architecture (SOA) або Microservices — якщо масштабувати Task-менеджер до вебдодатку з базою користувачів, сповіщеннями поштою, плануванням завдань.

Переваги мікросервісів:

незалежне розгортання частин системи;

масштабування окремих сервісів (наприклад, сповіщення можуть бути винесені в окрему службу);

гнучка інтеграція з іншими системами (через REST API або gRPC).

Але для настільного додатку або невеликого застосунку, мікроядерна архітектура — відмінне рішення.